# 引き継ぎ 2026-08-11（出発前日）

**明日 2026-08-12 が出発。** このファイルが最新の着地点。
`2026-08-10.md` は Phase B5 の実施記録と**確認手順の本体**として残してあるので、
実機確認をするときはそちらの「お土産リストページ: 合言葉を持つ人が確認すること」を見る。

**設計書（`docs/spec/travel-plans-redesign.md`）が常に正**で、ここは残タスクと順序だけを持つ。

## 現在地

| | |
|---|---|
| Phase A / B1 / B2 / B4 / B5 | 完了。main にマージ・push・本番反映済み |
| 本番の `events.json` | 封筒 JSON。40 イベント（2026-08-11 に更新されている） |
| 本番の `packing.json` | 封筒 JSON。39 項目 |
| 本番の `souvenirs.json` | 封筒 JSON。**9 件。2026-08-11 に初回公開が済んだ** |
| テスト | 567 pass / 0 fail |
| 出発 | **2026-08-12（明日）** |

お土産の 9 件は、渡された LINE スクリーンショット 6 枚と `omiyage.txt` から起こしたもの。
**旅程の行き先と突き合わせて「買えるか」をメモ欄に書いてある** ── 3 件は注意付き:

- スターバックスのタイ限定タンブラー: 投稿の店（モンティエン）は旅程外。他店でも買えるが売り切れ多い
- 大きめトートバッグ: 投稿はバンコクの Terminal 21、旅程で行くのは 8/16 のパタヤ店
- エコバッグ（チャトチャック市場）: **この旅程では行けない**（土日のみ・バンコク／8/15-16 はパタヤ）

**8/13(木) の Em District で 3 件が片付く**（Butterbear / BKK トート / DEAN&DELUCA）。

## 残タスク

### 1. 出発前に直したい UI の不具合 2 件（どちらも未修正）

ブランチ `fix/tbtn-hidden` に、**記録だけ**を先に入れてある（コードは未修正）。

**(a) `.tbtn` の `display` が `hidden` 属性に勝っている。** 「お土産を追加」「区分を追加」が
編集モードの外でも表示される。直しは `assets/css/controls.css` に
`.tbtn[hidden] { display: none; }` を足す 1 行。**同じ罠を `.syncbar` / `.pubpanel` /
`.pubstat` では既に回避してあり、`.tbtn` にだけ入れ忘れていた**（設計書 §13）。

**(b) チェックボックスの縦位置。** `.svitem` が `align-items: center` なので、メモを持つ行で
チェックが名前の行から下にずれて見える。`flex-start` にすると読みやすい。

どちらも見た目だけで、データは壊れない。**出発前に触るかどうかは判断が要る** ──
(a) は 1 行だが、`packing.html` の見え方も変わる。

### 2. 6 名向けの実機確認（未実施）

`2026-08-10.md` の 11 手順。**合言葉が要るので人にしかできない。** とくに:

- **手順 4**（「何を」に打った直後、間を置かず 1 回だけ「お土産を追加」）── テストが
  原理的に捕まえられない罠。1 回で増えなければ重大
- **手順 11**（旅程・持ち物が無事か）── B5 は `schedule.js` と `packing.js` を書き換えている
- **持ち物ページの ↑↓ でフォーカスが残るか** ── B5 でフォーカスキーの作り方を変えた

### 3. Phase B3（コメント）── 着手は帰国後を勧める

設計書 §4.3 / §7.5。`event:<id>` / `day:<n>` / `packing:<id>` / `global` の 4 対象。

**出発前日に始めないほうがよい理由が 2 つある。** 4 か所すべて（予定シート・日付ヘッダー・
持ち物の行・旅程ページ）に手を入れるので、いま動いている 3 ページ全部を触ることになる。
それに、予定・持ち物・お土産にはすでに `note` 欄があり、B3 が足すのは「誰が・いつ」と
「1 対象に複数件」だけ ── 旅行中に本当に要るかは、使ってみないと分からない。

共通部品（`page-notice.js` / `focus-key.js` / `row-controls.js`）は B5 で揃えたので、
4 本目は写さずに書ける。`assets/js/souvenirs.js` の `createSync()` / `createPublishUI()` の
呼び出しをそのまま写すこと（注入口は 6 つ、`content` は組で渡す）。

### 4. 設計書 §13 の繰り越し

B5 で carry と判定した 10 件（初公開前の offline バー、読み込み失敗時の空ボタン、など）。
いずれも `packing.js` と共通の既存挙動で、出発前に触るリスクのほうが大きいという判断。

## 今日わかった、次に効くこと

**エントリポイントと CSS の組み合わせは、読んで判定せずにブラウザで開いて確かめること。**

(a) の不具合は、Phase B5 の全工程 ── 9 タスク、レビュー 4 回、最終の全体レビュー ──
を素通りした。`node --test` は CSS の詳細度勝負を見られず、レビューは JS だけを読んで
「隠れている」と判定した。さらに悪いことに、**その判定を信じて確認手順を
「ボタンは表示されない」と書き換えてしまった**（もとの記述のほうが実態に合っていた）。

実際にページを開いた 30 秒で見つかっている。`souvenirs.js` / `packing.js` /
`schedule.js` はテストを持たない規約なので、**ここだけは人かブラウザが見るしかない。**
