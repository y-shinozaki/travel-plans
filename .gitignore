# OS
.DS_Store
Thumbs.db
*.swp
*.swo
*~

# IDE
.vscode/
.idea/
*.sublime-project
*.sublime-workspace

# Environment
.env
.env.local

# Dependencies (if added in future)
node_modules/
package-lock.json
yarn.lock

# Build outputs (if added in future)
dist/
build/
.parcel-cache/

# Logs
*.log
npm-debug.log*

# 作業用スクリーンショット。**リポジトリ直下だけ**を対象にする。
# かつては `*.png` / `*.jpg` と全階層を除外していたが、それだと
# assets/ に画像を 1 枚置いた瞬間に黙って追跡対象から外れる
# （実際、セルフホストした Leaflet のマーカー画像を通すために
# `!assets/vendor/leaflet/images/*.png` という例外が要っていた）。
/*.png
/*.jpg
.playwright-mcp/

# Temp
.tmp/

# 手元にだけ置く元データ（LINE のトーク履歴、メールの控えなど）
private/

# Claude Code / Superpowers の作業領域（どちらも自動生成される）
.superpowers/
.claude/worktrees/
